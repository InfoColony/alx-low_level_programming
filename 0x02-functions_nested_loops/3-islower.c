#include "main.h"

/**
 * _islower - a function used to check if a character is lower case.
 * C - the argument or parameter use i
 * Return: it retuns the number of c.
 */

/* parameter to print c number */
int _islower(int c)
{
	/** the variable for c */
	if (c > 90)
		return (1);
	else
		return (0);
}
