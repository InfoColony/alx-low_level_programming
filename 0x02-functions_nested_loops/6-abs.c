#include "main.h"

/**
 * _abs - A function that return the absolute value of an input
 * @i: the input to be returned
 * Return: Always 0
 */


int _abs(int i)
{
	if (i <= 0 || i >= 0)
		return (i);
	return (0);
}
