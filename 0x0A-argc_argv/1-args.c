#include <stdio.h>

/**
 * main - Entry point of the program
 * @c: an input to the program
 * @arg: an input to the function
 * Return: return 0 on success
 */

int main(int c, char *arg[] __attribute__((unused)))
{
	printf("%d\n", c - 1);
	return (0);
}
