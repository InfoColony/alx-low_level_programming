#include "main.h"

/**
 * mul - A function that multiply's two inputs and return their result
 * @a: first input
 * @b: second input
 * Return: return the multiplied result
 */


int mul(int a, int b)
{
	int r = a * b;

	return (r);
}
